import { DatadetailPipe } from './datadetail.pipe';

describe('DatadetailPipe', () => {
  it('create an instance', () => {
    const pipe = new DatadetailPipe();
    expect(pipe).toBeTruthy();
  });
});
